# Database connection
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=knowledgeplan_dev
POSTGRES_HOST=db
POSTGRES_PORT=5432
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# Application settings
SECRET_KEY=changeme_use_a_strong_random_secret_key_in_production
ENVIRONMENT=development
DEBUG=true
ALLOWED_HOSTS=localhost,127.0.0.1

# API settings
API_V1_PREFIX=/api/v1

# JWT settings
JWT_SECRET_KEY=changeme_use_another_strong_secret_for_jwt_tokens
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30

# LLM integration
LLM_API_KEY=your_llm_api_key
LLM_API_ENDPOINT=https://api.llm-provider.com/v1

# Google Calendar integration
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_REDIRECT_URI=http://localhost:8001/api/v1/auth/google/callback