# .env file for knowledgeplan-backend
# Add environment variables here (copy this to .env and fill in actual values)

# Example Database Connection (replace with your actual local/dev DB)
# POSTGRES_SERVER=localhost
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=your_db_password
# POSTGRES_DB=knowledgeplan_dev

# Example Security
SECRET_KEY=a_very_secret_key_change_this # IMPORTANT: Generate a real secret key!

# Google OAuth (Obtain from Google Cloud Console)
GOOGLE_CLIENT_ID=your_google_client_id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your_google_client_secret 